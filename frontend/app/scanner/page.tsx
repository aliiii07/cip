import { ComingSoon } from "@/components/ComingSoon";

export default function Scanner() {
  return (
    <ComingSoon
      page="Scanner"
      phase="P4"
      description="24/7 watchlist scan hub: radar sweep per bar close, price action, trend, volume, volatility, and news context cards, plus the Market Scout signal relationship graph."
    />
  );
}

